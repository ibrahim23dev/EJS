<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>FlyGhor_Invoice_<%= invoiceNo %></title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;500;600;700;800&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Plus Jakarta Sans', sans-serif;
            background-color: #f3f4f6;
            margin: 0;
            padding: 0;
        }

        .invoice-wrapper {
            position: relative;
            width: 210mm;
            height: 297mm;
            margin: 20px auto;
            background: white;
            padding: 15mm 20mm;
            box-shadow: 0 4px 20px rgba(0,0,0,0.05);
            overflow: hidden;
            box-sizing: border-box;
            display: flex;
            flex-direction: column;
            justify-content: space-between;
        }

        .watermark {
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%) rotate(-45deg);
            font-size: 120px;
            font-weight: 700;
            color: rgba(0,0,0,0.02);
            letter-spacing: 5px;
            z-index: 10;
            pointer-events: none;
        }

        .content {
            position: relative;
            z-index: 2;
            flex: 1;
            display: flex;
            flex-direction: column;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            margin: 15px 0;
        }

        th, td {
            padding: 8px 0;
            text-align: left;
            border-bottom: 1px solid #e5e7eb;
            font-size: 12px;
        }

        th {
            font-weight: 700;
            color: #4b5563;
            text-transform: uppercase;
            letter-spacing: 0.05em;
        }

        .section-title {
            font-size: 10px;
            font-weight: 800;
            color: #3b82f6;
            text-transform: uppercase;
            letter-spacing: 0.1em;
            margin-bottom: 8px;
        }

        @media print {
            body { background: white; padding: 0; margin: 0; }
            .invoice-wrapper { 
                margin: 0; 
                padding: 15mm 20mm; 
                width: 100%; 
                height: auto; 
                box-shadow: none; 
                page-break-after: avoid;
            }
            @page { size: A4 portrait; margin: 0; }
            .no-print { display: none; }
        }
    </style>
</head>
<body>
    <div class="invoice-wrapper">
        <div class="watermark">FLYGHOR</div>
        <div class="content">
            <!-- Header -->
            <div class="flex justify-between items-start mb-8">
                <div>
                    <h1 class="text-3xl font-bold text-gray-900">FLY<span class="text-blue-600">GHOR</span></h1>
                    <p class="text-xs text-gray-500 uppercase tracking-wide mt-1">Reliable Travel Solutions</p>
                    <div class="mt-4 text-sm">
                        <p>PNR: <span class="font-bold"><%= pnr %></span></p>
                        <p>Ref: <%= bookingRef %></p>
                    </div>
                </div>
                <div class="text-right">
                    <h2 class="text-2xl font-bold text-gray-300 uppercase">Invoice</h2>
                    <div class="mt-2 bg-gray-800 text-white p-3 rounded-md inline-block">
                        <p class="text-xs uppercase opacity-70">#<%= invoiceNo %></p>
                        <p class="text-sm font-bold"><%= bookingDate %></p>
                    </div>
                </div>
            </div>

           
            <div class="grid  grid-cols-2 gap-8 mb-8">
                <div>
                    <h3 class="section-title">B2B Agent Info</h3>
                    <p class="font-bold text-lg"><%= agency.name %></p>
                    <p class="text-xs text-gray-500">Agent ID: <%= agency.code %></p>
                    <p class="text-sm text-gray-600 mt-2"><%= agency.address %></p>
                    <p class="text-sm font-medium mt-1">Attn: <%= agency.contactPerson %></p>
                    <p class="text-sm"><%= agency.email %> | <%= agency.phone %></p>
                </div>
                <div class="p-4 rounded-md border-[1px]">
                    <h3 class="section-title">Passenger Contact Info</h3>
                    <p class="font-medium"><%= contact_name %></p>
                    <p class="text-sm text-gray-600"><%= contact_phone %></p>
                    <span class="inline-block mt-2 px-2 py-1 bg-green-100 text-green-700 text-xs rounded-full font-medium">Status: <%= ticketStatus %></span>
                </div>
            </div>

            
            <table>
                <thead>
                    <tr>
                        <th>#</th>
                        <th>Traveler Name</th>
                        <th>Pax Type</th>
                        <th>Passport</th>
                        <th class="text-right">Nationality</th>
                    </tr>
                </thead>
                <tbody>
                    <% passengers.forEach((pax, index) => { %>
                    <tr>
                        <td><%= index + 1 %></td>
                        <td>
                            <span class="font-medium"><%= pax.name %></span>
                            <span class="text-xs text-gray-500 block"><%= pax.gender %> • DOB: <%= pax.dob %></span>
                        </td>
                        <td><%= pax.type %></td>
                        <td><%= pax.passport %></td>
                        <td class="text-right uppercase"><%= pax.nationality %></td>
                    </tr>
                    <% }) %>
                </tbody>
            </table>

            <!-- Footer Section -->
            <div class=" mt-20 pt-6 border-t border-gray-200 grid grid-cols-2 gap-8">
                <div>
                    <h3 class="section-title">Fare Policy</h3>
                    <p class="text-sm text-gray-600 italic"><%= fareRulesSummary %></p>
                    <p class="text-xs text-gray-500 mt-4"><strong>Remarks:</strong> <%= remarks %></p>
                    <p class="text-xs font-bold mt-2">Amount in words: <%= amountInWords %> Only</p>
                </div>
                <div class="text-right">
                    <div class="space-y-1 text-sm">
                        <div class="flex justify-between"><span class="text-gray-600">Base Fare</span><span><%= currency %> <%= price.baseFare %></span></div>
                        <div class="flex justify-between"><span class="text-gray-600">Taxes & Fees</span><span><%= currency %> <%= price.taxes %></span></div>
                        <div class="flex justify-between text-red-600"><span>Agent Comm (-)</span><span><%= currency %> <%= price.commissionAmount %></span></div>
                        <div class="flex justify-between text-blue-600"><span>Markup (+)</span><span><%= currency %> <%= price.markup %></span></div>
                        <div class="border-t pt-2 mt-2 flex justify-between font-bold text-lg">
                            <span>Total Payable</span>
                            <span><%= currency %> <%= price.totalAmount %></span>
                        </div>
                    </div>
                </div>
            </div>

          
            <div class="flex justify-between items-center mt-6 text-xs text-gray-400">
                <div class="flex items-center gap-2">
                    <img src="https://api.qrserver.com/v1/create-qr-code/?size=60x60&data=FlyGhor-INV-<%= invoiceNo %>" alt="QR" class="w-8 h-8 opacity-50">
                    <span>Digital Verification</span>
                </div>
                <div class="text-center">
                    <div class="border-b border-gray-800 w-32 mx-auto mb-1"></div>
                    <span>Digitally Signed by FlyGhor</span>
                </div>
                <span>flyghor.com</span>
            </div>
        </div>
    </div>

    <button onclick="window.print()" class="no-print fixed bottom-4 right-4 bg-blue-600 text-white px-6 py-2 rounded-md shadow-lg hover:bg-blue-700">
        Print / PDF
    </button>
</body>
</html>